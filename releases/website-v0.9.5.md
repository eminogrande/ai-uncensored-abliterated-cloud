# ABLITERATED.cloud website v0.9.5

- **Newest-first landing page**: the "Latest uncensored releases" section now
  leads immediately after the hero — the newest and best models (Qwen3.8,
  Muse-Glimmer, DeepSeek V4 Flash, ...) with price estimates and ZERO
  REFUSALS badges come first, the hosted catalog follows as "The four models
  we host today". The hero badge and meta description now say "the newest
  abliterated releases, covered daily" instead of "four models".
- **Learn sections on every post**: all 14 field notes now include "The idea,
  in plain words" — a short, non-technical-but-technical explainer of one
  core concept each model embodies (mixture of experts, 4-bit quantization,
  knowledge distillation, multi-token prediction, the refusal-direction
  lambda dial, KL divergence, tool-calling fine-tuning, VRAM math, license
  meaning, vision encoders). Mirrored in the Markdown twins.
- The daily editorial pipeline now requires the learn section on every post.
